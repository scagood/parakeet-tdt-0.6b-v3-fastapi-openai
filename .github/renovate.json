{
  "$schema": "https://docs.renovatebot.com/renovate-schema.json",
  "extends": [
    "local>darksheepsoftware/.github:renovate-config"
  ],
  "customManagers": [
    {
      "customType": "regex",
      "description": "Both Dockerfiles pin pip wheels that no built-in manager sees: the CPU onnxruntime build and the CPU torch pair",
      "managerFilePatterns": [
        "/(^|/)Dockerfile\\.(cpu|gpu)$/"
      ],
      "matchStrings": [
        "(?<depName>onnxruntime|torchaudio|torch)==(?<currentValue>[0-9][0-9a-zA-Z.\\-]*)"
      ],
      "datasourceTemplate": "pypi"
    }
  ],
  "packageRules": [
    {
      "description": "Move the CPU and GPU ONNX Runtime wheels in one PR so the images stay on the same build",
      "matchDatasources": [
        "pypi"
      ],
      "matchPackageNames": [
        "onnxruntime",
        "onnxruntime-gpu"
      ],
      "groupName": "onnxruntime"
    },
    {
      "description": "silero-vad caps torchaudio below 2.10 and each torchaudio pins its exact torch. Past that cap the requirements install downgrades torchaudio and drags torch plus the CUDA wheels in from PyPI, so hold the pair below it and move both in one PR.",
      "matchDatasources": [
        "pypi"
      ],
      "matchPackageNames": [
        "torch",
        "torchaudio"
      ],
      "allowedVersions": "<2.10",
      "groupName": "torch"
    }
  ]
}
