onnx-asr[hub]>=0.10.0
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
python-multipart>=0.0.9
silero-vad>=6.0.0
psutil>=5.0.0
requests>=2.0.0
onnxruntime-gpu[cuda,cudnn]==1.30.0
numpy<3

# CPU-only deployments replace onnxruntime-gpu with onnxruntime and run with:
# PARAKEET_USE_GPU=false PARAKEET_BATCHED=0
# (PARAKEET_USE_GPU=false already selects the CPU default, parakeet-v3-fp32)
